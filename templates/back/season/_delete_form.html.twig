<form method="post" action="{{ path('back_season_delete', {'id': season.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer cette saison ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
